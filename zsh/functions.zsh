# =============================================================================
# Custom shell functions (zsh)
# Deprecated: sources from shared/ for consistency.
# If you need zsh-specific overrides, add them below the source line.
# =============================================================================

# Load cross-shell functions (bash & zsh compatible)
[ -f "$HOME/.dotfiles/shared/functions.sh" ] && source "$HOME/.dotfiles/shared/functions.sh"
