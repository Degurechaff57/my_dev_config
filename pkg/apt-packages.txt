# =============================================================================
# Linux (Debian/Ubuntu) — APT dependencies
# Install: grep -v '^#' apt-packages.txt | grep -v '^$' | xargs sudo apt install -y
# =============================================================================

# Build essentials
build-essential
git
curl
wget
unzip

# Shell enhancements (bash-native, no zsh needed)
bash-completion
tmux

# Dev tools
python3
python3-pip
python3-venv
nodejs
npm
ripgrep
fd-find
fzf
bat
eza
zoxide
jq
tree

# NeoVim
neovim

# Clipboard (for tmux cross-platform copy)
xclip
wl-clipboard

# Fonts (for terminal icons)
fonts-jetbrains-mono
